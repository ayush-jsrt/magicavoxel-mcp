`ff3.vox` is vendored from [ephtracy/voxel-model](https://github.com/ephtracy/voxel-model)
(`vox/procedure/ff3.vox`), MIT licensed, © ephtracy. Used here as a real
MagicaVoxel-authored file to validate `vox_io.read_vox` against actual output
from the app, not just our own writer's round-trip.
